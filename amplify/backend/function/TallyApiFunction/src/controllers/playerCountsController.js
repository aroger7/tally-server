const App = require('../models/App');
const PlayerCount = require('../models/PlayerCount');

const { pickDefined } = require('../services/pickDefined');

exports.getPlayerCounts = (req, res) => {
  
};